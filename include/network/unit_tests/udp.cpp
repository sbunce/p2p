//include
#include <network/network.hpp>

int main()
{
/*
	std::set<network::endpoint> E = network::get_endpoint("", "8888", network::udp);
	assert(!E.empty());
	network::nstream N(*E.begin());
	network::buffer buf;
	N.recv(buf);
	LOGGER << buf.str();
*/
}
