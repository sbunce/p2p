def build(bld):
	bld(
		features = ['cxx', 'cprogram'],
		source = bld.path.ant_glob('*.cpp'),
		target = 'http',
		use = ['platform', 'boost'],
		uselib_local = [
			'local_include',
			'net'
		]
	)
